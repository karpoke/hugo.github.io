#!/bin/bash
# Pre-commit hook to validate the Hugo build
# Only runs when staged files could affect the site output

# Patterns that require a Hugo build validation
HUGO_PATTERNS='content/|hugo\.toml|layouts/|assets/|static/|i18n/|data/|themes/'

# Check if any staged file matches a Hugo-relevant pattern
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACMR)
HUGO_FILES=$(echo "$STAGED_FILES" | grep -E "$HUGO_PATTERNS")

if [ -z "$HUGO_FILES" ]; then
    echo "⏭️  Pre-commit hook: No Hugo content changes, skipping build"
    exit 0
fi

echo "🔍 Pre-commit hook: Validating Hugo build..."
echo "   Changed files affecting site: $(echo "$HUGO_FILES" | wc -l)"

# Verify Hugo is installed
if ! command -v hugo &> /dev/null; then
    echo "❌ Error: Hugo is not installed"
    echo "   Install Hugo from: https://gohugo.io/installation/"
    exit 1
fi

# Run hugo build to validate
if hugo --quiet --minify 2>&1 | tee /tmp/hugo-build.log; then
    echo "✅ Hugo build successful"
    # Clean up generated files (we don't want to commit them)
    rm -rf public/ resources/
    exit 0
else
    echo "❌ Error: Hugo build failed"
    echo ""
    echo "Errors found:"
    cat /tmp/hugo-build.log
    echo ""
    echo "Please fix the errors before committing."
    echo "You can run 'hugo --verbose' for more details."
    exit 1
fi
